# MySQL
MYSQL_ROOT_PASSWORD=changeme
MYSQL_USER=app
MYSQL_PASSWORD=changeme

# Auth
AUTH_JWT_SECRET=change-me-to-a-random-32-char-secret

# Redis
# Leave empty if Redis runs without authentication (default in compose).
REDIS_PASSWORD=

# App environment
# local | dev | prod
APP_ENV=local
