meta {
  name: List Tasks
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/tasks
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  team_id: {{teamId}}
  ~status: todo
  ~assignee_id:
  ~page: 1
  ~limit: 20
}
